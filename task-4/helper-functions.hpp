#ifndef HELPER_FUNCS_H
#define HELPER_FUNCS_H

// Notice that in the header file, if we want to use strings or cout, we must
// still include the corresponding files
#include <string>

// Function: StringLength
// Parameters: A single string
// Purpose: Figures out how long the string is
// Return: Returns the length of the given string
int StringLength(std::string word);

#endif